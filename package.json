{
  "name": "@valen-systems/core",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix runtime run serve",
    "demo:william": "bash scripts/william-demo.sh",
    "check": "npm --prefix runtime run check && npm --prefix runtime run proof:bundle && node scripts/check-sensitive.mjs",
    "proof:browser": "npm --prefix runtime run proof:browser"
  }
}
